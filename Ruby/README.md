# QuickGuide | Ruby

```ruby
first_name = "John"
last_name = "Doe"
full_name = first_name + " " + last_name
puts full_name
```

```ruby
# (a + b)^2 = a^2 + b^2 + 2ab

a = 10
b = 20

left_expression = (a + b) ** 2

right_expression = a**2 + b**2 + 2*a*b

result = left_expression == right_expression

puts result
```
